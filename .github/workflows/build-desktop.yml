name: Build Desktop

on:
  push:
    tags:
      - 'v*.*.*'

concurrency:
  group: build-desktop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-config:
    name: Validate Tauri Config
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate tauri.conf.json against schema
        run: |
          npm install -g ajv-cli ajv-formats
          curl -sL "https://raw.githubusercontent.com/tauri-apps/tauri/dev/crates/tauri-config-schema/schema.json" -o /tmp/tauri-schema.json
          ajv validate -s /tmp/tauri-schema.json -d apps/web/src-tauri/tauri.conf.json --spec=draft2020 -c ajv-formats

  build-windows:
    name: Build Windows Installer
    needs: validate-config
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/web/src-tauri -> target

      - name: Install dependencies
        run: pnpm install

      - name: Set version from tag
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          cd apps/web/src-tauri
          sed -i "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" tauri.conf.json
          sed -i "s/^version = \".*\"/version = \"$VERSION\"/" Cargo.toml

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: apps/web
          tagName: ${{ github.ref_name }}
          releaseName: Kover ${{ github.ref_name }}
          releaseBody: |
            ## Kover ${{ github.ref_name }}

            ### Установка
            Скачайте `Kover-Setup-${{ github.ref_name }}.exe` и запустите установщик.

            ### Обновление
            Если у вас уже установлен Kover — обновление произойдёт автоматически.

            ### KoverBot (Telegram AI-бот)
            - `KoverBot-install.bat` — установка бота на Windows 11 (WSL2)
            - `KoverBot-update.bat` — обновление бота
          releaseDraft: false
          prerelease: false
          updaterJsonPreferNsis: true

      - name: Upload KoverBot scripts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          gh release upload "${{ github.ref_name }}" \
            scripts/koverbot-install.bat \
            scripts/koverbot-update.bat \
            --clobber

  build-macos:
    name: Build macOS (${{ matrix.target }})
    needs: validate-config
    strategy:
      matrix:
        include:
          - target: aarch64-apple-darwin
            runner: macos-latest
          - target: x86_64-apple-darwin
            runner: macos-latest
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/web/src-tauri -> target

      - name: Install dependencies
        run: pnpm install

      - name: Set version from tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          cd apps/web/src-tauri
          sed -i '' "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" tauri.conf.json
          sed -i '' "s/^version = \".*\"/version = \"$VERSION\"/" Cargo.toml

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: apps/web
          tagName: ${{ github.ref_name }}
          releaseName: Kover ${{ github.ref_name }}
          args: --target ${{ matrix.target }}
          releaseDraft: false
          prerelease: false
