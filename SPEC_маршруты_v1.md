# SPEC: Система управления маршрутами
## Сервис аренды грязезащитных ковриков

**Версия:** 1.1  
**Дата:** Февраль 2026  
**Статус:** Черновик для согласования

---

## 1. Обзор проекта

### 1.1 Проблема

Управление маршрутами и клиентской базой ведётся в Excel-файле «Илья_новый.xls» со сложной структурой: 11 листов, дублирование данных, ручной подсчёт метража и распределение по дням.

**Ключевые проблемы текущего файла:**

- Данные о клиенте (название, адрес, **размер ковриков**) записаны в одну ячейку произвольным текстом — размер коврика является частью имени клиента. Примеры:
  - `метрополис к. Маркса -2x180, 2x150`
  - `Сбер академия 2х150 коричнев стар`
  - `пальмира Ленина 5- 200 новый 150 нов`
- Распределение по дням дублируется на нескольких листах (лист «Неделя» + листы-фильтры «Понедельник»–«Пятница» + маршрутные листы «ПН»–«ПТ»)
- Порядок объезда задаётся дробными числами (1, 2, 3... 16.5, 27.05, 27.491) — вставки между точками без перенумерации
- Маршрут на день — единый упорядоченный список точек (без деления на блоки/подмаршруты)
- Нет защиты от ошибок: легко потерять клиента, забыть точку, перепутать коврик
- Конечный пользователь (Илья, 55 лет) испытывает затруднения при работе с файлом

### 1.2 Цель

Создать **веб-приложение**, которое:

1. Заменит Excel-файл единым интерфейсом с базой клиентов и маршрутами
2. Будет максимально простым для пользователя 55+ лет
3. Автоматически считает метраж и количество ковриков по дням
4. Позволит менять порядок объезда, переносить клиентов между днями
5. Даст возможность распечатать маршрутный лист
6. Сохраняет данные между сессиями

### 1.3 Пользователи

**Основной пользователь: Администратор (владелец бизнеса)** — управляет базой клиентов, маршрутами, формирует маршрутные листы. Работает с компьютера, локально, одно устройство.

**Вторичный пользователь: Илья (курьер, ~55 лет)** — получает готовый маршрутный лист на день, выполняет объезд. Интерфейс для курьера = печатный лист (максимально крупный шрифт, простая таблица).

- **Критерий для админа:** полное управление базой и маршрутами через удобный веб-интерфейс
- **Критерий для Ильи:** получить понятный печатный маршрутный лист, разобраться без объяснений

### 1.4 Формат решения

Веб-приложение (React), открывается в браузере. Один HTML-файл — без установки, без серверов, без логинов.

---

## 2. Анализ текущих данных

### 2.1 Структура файла

| Листы | Назначение | Описание |
|-------|-----------|----------|
| Неделя | Мастер-таблица | 143 клиента: частота, флаги по дням, метраж |
| ПН, ВТ, СР, ЧТ, ПТ | **Маршрутные листы** | Упорядоченные точки с номером остановки и раскладкой ковриков. |
| Понедельник–Пятница | Фильтры по дням | Дублирование мастер-таблицы, отфильтрованное по флагу дня |

### 2.2 Структура записи клиента

| Поле | Как хранится сейчас | Пример |
|------|-------------------|--------|
| Название + адрес + коврики | Одна ячейка, свободный текст | `метрополис к. Маркса -2x180, 2x150` |
| Общий метраж (кв.м) | Число | `6.690` |
| Кол-во ковриков по размерам | Отдельные столбцы: 180, 150, 60×80, 400, 250 | `2, 2, 0, 0, 0` |
| Частота (раз/нед) | Число 1–5 | `3` |
| Дни обслуживания | Флаги 0/1 | `ПН=1, ВТ=0, СР=1, ЧТ=0, ПТ=1` |
| Порядок в маршруте | Дробное число (per day) | `16.5`, `27.491` |
| Цвет/тип коврика | Вшит в текст названия | `коричневый`, `серый`, `новый`, `старый` |
| Доп. информация | Вшита в текст | `3-ий вход`, `резаный` |

### 2.3 Статистика базы

| Показатель | Значение |
|-----------|----------|
| Всего клиентов | 143 |
| Общий метраж (все коврики) | ~98.5 кв.м |
| Размеры ковриков | 180, 150, 60×80, 400, 250 (+ резаные) |
| Точек в ПН | 58 точек, ~141 кв.м |
| Точек во ВТ | 58 точек, ~148 кв.м |
| Точек в СР | 49 точек, ~132 кв.м |
| Точек в ЧТ | 56 точек, ~139 кв.м |
| Точек в ПТ | 59 точек, ~138 кв.м |

**Распределение по частоте:**

| Частота | Клиентов | Примечание |
|---------|---------|------------|
| 5 раз/нед | 8 | Каждый день |
| 4 раза/нед | 4 | ПН–ЧТ |
| 3 раза/нед | 24 | Обычно ПН/СР/ПТ |
| 2 раза/нед | 44 | Разные комбинации |
| 1 раз/нед | 63 | Самая большая группа |

### 2.4 Особенности маршрутов

**Плоский список:** Маршрут на каждый день — один упорядоченный список точек без деления на подмаршруты или блоки. В исходном Excel (листы ПН–ПТ) маршрут представлен как единая последовательность остановок.

**Дробная нумерация:** Вставки между точками делаются через дробные числа (16.5 = между 16 и 17, 27.491 = после 27.49). В приложении это должно решаться drag & drop — без дробей.

---

## 3. Модель данных

### 3.1 Клиент (Client)

```
{
  id: number              // авто-генерация
  originalName: string    // "метрополис к. Маркса -2x180, 2x150" — исходная строка из Excel, используется для отображения в маршруте и печатном листе
  name: string            // "Метрополис" — чистое название (для базы клиентов)
  address: string         // "К. Маркса, д.5"
  mats: [                 // массив ковриков
    { size: "180", qty: 2, color: "стандарт" },
    { size: "150", qty: 2, color: "коричневый" }
  ]
  totalSqm: number        // авто-расчёт: 6.690
  frequency: number       // 1–5
  days: string[]          // ["ПН", "СР", "ПТ"]
  notes: string           // "3-ий вход, резаный"
  active: boolean         // true/false (на паузе или нет)
}
```

### 3.2 Маршрут дня (DayRoute)

```
{
  day: string             // "ПН"
  stops: [                // упорядоченные точки
    { order: 1, clientId: 5 },
    { order: 2, clientId: 12 },
    ...
  ]
  totalSqm: number        // авто-сумма
  totalMats: {             // авто-подсчёт
    "180": 23, "150": 19, "60x80": 1, "400": 1, "250": 1
  }
}
```

### 3.3 Справочник размеров ковриков

| Размер | Площадь (кв.м) | Примечание |
|--------|----------------|------------|
| 180 (120×150) | 2.070 | Основной размер |
| 150 (85×150) | 1.275 | Стандартный |
| 60×80 | 1.275 | Нестандартный |
| 400 (резаный) | ~4.000 | Крупный |
| 250 | 3.700 | Крупный |
| 200 | 2.070 | Встречается у отдельных клиентов |
| 240 | 2.700 | Редкий |

> **Примечание:** Точные площади нужно уточнить с Ильёй. Данные выше — расчёт из файла.

---

## 4. Экраны приложения

### 4.1 Главный экран — Маршрут на сегодня

**При открытии приложение сразу показывает маршрут на текущий день** — это то, зачем админ открывает приложение в 90% случаев.

- Вверху: переключатель дней — 5 кнопок (ПН | ВТ | СР | ЧТ | ПТ), текущий день подсвечен
- Рядом с каждым днём: мини-бейдж с количеством точек
- **Сводка дня (блок вверху, на видном месте):**
  - Количество точек
  - **Сколько ковриков нужно подготовить** — по каждому размеру отдельно (напр. «180: 23 шт | 150: 19 шт | 60×80: 1 шт»)
  - Общий метраж
- Это **ключевая сводка** — именно её админ использует вечером, чтобы подготовить стопку ковриков на завтра

> **Почему не дашборд с карточками:** Админ открывает приложение с конкретной целью — посмотреть маршрут на день или отредактировать базу. Отдельный дашборд с 5 карточками — лишний клик без пользы. Переключатель дней вверху маршрута решает ту же задачу.

### 4.2 Маршрут на день (список точек)

**Под сводкой — список точек в порядке объезда:**

- Каждая точка — строка/карточка: `№ | Название (как в Excel) | коврики по размерам | метраж`
- **Поле поиска** прямо над списком — фильтрует точки по названию/адресу. При 58 точках скроллить без поиска — мучение
- Чекбокс «✓ Выполнено» напротив каждой точки
- Кнопки ↑↓ для перемещения + drag & drop

**Действия на экране маршрута:**

**Изменить порядок:** кнопки ↑↓ или drag & drop. Нумерация пересчитывается автоматически (без дробей).

**Добавить точку в маршрут:**
1. Кнопка «+ Добавить точку» → открывается модальное окно со списком клиентов (только те, кто ещё НЕ в этом маршруте)
2. Поиск по названию в модалке
3. Клик на клиента → выбор позиции: «Вставить после точки №___» (выпадающий список) или «В конец»
4. Клиент появляется в маршруте в выбранной позиции, нумерация пересчитывается

**Убрать точку из маршрута:** кнопка ✕ на карточке точки → подтверждение → точка убрана, нумерация пересчитана. Клиент НЕ удаляется из базы.

**Перенести точку в другой день:**
1. Кнопка «Перенести» на карточке точки
2. Выбор дня (ПН | ВТ | СР | ЧТ | ПТ) + позиция в новом маршруте
3. Точка убирается из текущего дня и появляется в новом

**Отображение названий:** В маршруте точка показывается **в привычном формате из Excel** (например, «Монетка гоголя 103 -180»), чтобы Илья узнавал клиентов. Структурированные поля (отдельно название, адрес, коврики) — только в базе клиентов.

### 4.3 База клиентов

**Управление всеми клиентами:**

- Таблица с поиском (поиск по названию и адресу)
- Столбцы: название, адрес, коврики, метраж, частота, дни, статус
- Фильтры: по дню недели, по частоте, по размеру коврика
- Сортировка по любому столбцу

**Действия:**
- «Добавить клиента» — форма с полями:
  - Название (текст)
  - Адрес (текст)
  - Коврики (кнопка «+ коврик» → выбрать размер, кол-во, цвет)
  - Частота (выпадающий: 1–5)
  - Дни (чекбоксы: ПН ☐ ВТ ☐ СР ☐ ЧТ ☐ ПТ ☐)
  - Примечания (текст)
- Редактировать клиента (клик на строку → та же форма)
- Удалить клиента (с подтверждением)
- Поставить на паузу / активировать

### 4.4 Печатный маршрутный лист

**Это основной рабочий инструмент Ильи.** Он носит его весь день, складывает, мнёт, пачкает. Поэтому формат критичен.

Генерируется кнопкой «Печать» на экране маршрута → CSS @media print → браузерная печать.

- Заголовок: день недели + дата печати
- **Название точки — в привычном формате из Excel** (единая строка «Монетка гоголя 103 -180»), а не разбитое на поля. Илья узнаёт клиентов именно по этим строкам.
- Таблица:

| № | Название | 180 | 150 | 60×80 | 400 | 250 | Кв.м | ✓ |
|---|---------|-----|-----|-------|-----|-----|------|---|
| 1 | сбербанк гоголя 98-3ий вход 1х150 | 2 | 5 | | | | 1.275 | ☐ |
| 2 | газпроммежрегионгаз- 2x150, 180 | 1 | 2 | | | | 4.620 | ☐ |

- Строка итогов в конце (количество по каждому размеру + метраж)
- Крупный шрифт: **14pt минимум**
- Чёрно-белая (экономия тонера)
- Поле «✓» — пустой квадрат для ручной отметки ручкой
- На печати скрываются все элементы интерфейса (кнопки, меню) — только таблица

---

## 5. UX-требования

### 5.1 Админ-интерфейс (основной пользователь)

Админ — технически грамотный владелец бизнеса. Интерфейс должен быть удобным и функциональным, но не обязательно «упрощённым до предела». Фокус на эффективность:

| Параметр | Требование |
|----------|-----------|
| Шрифт | 14–16px основной, 18px+ заголовки |
| Кнопки | Чёткие, подписанные |
| Навигация | Вкладки сверху, максимум 2 клика до любой функции |
| Действия | Автосохранение, подтверждение удаления |
| Обратная связь | «✓ Сохранено» на 2 секунды |

### 5.2 Печатный лист для курьера (Илья, 55 лет)

Это единственный интерфейс, с которым работает Илья. Требования максимальной простоты:

| Параметр | Требование |
|----------|-----------|
| Шрифт | **Минимум 14pt**, заголовки 18pt+ |
| Формат | Таблица на A4, чёрно-белая |
| Информация | Только нужное: номер, название, адрес, коврики, метраж, галочка |
| Итоги | Сумма по каждому размеру + общий метраж |
| Поле «✓» | Пустой квадрат для ручной отметки |

### 5.3 Взаимодействие

- Перемещение точек: **кнопки ↑↓** (основной способ) + drag & drop
- Удаление: подтверждение «Точно удалить? ДА / НЕТ»
- **Автосохранение** — никаких кнопок «Сохранить»
- Ошибки: понятным языком, с подсказкой что делать

---

## 6. Рабочие сценарии (User Stories)

Это конкретные ситуации, которые приложение должно обрабатывать. Каждый сценарий — реальная ежедневная задача.

### US-1: Подготовка ковриков на завтра (вечерний ритуал)

**Ситуация:** Вечером админ готовит стопку ковриков для завтрашнего маршрута.

**Шаги:**
1. Открывает приложение → видит маршрут на сегодня
2. Переключает на завтрашний день (клик по вкладке)
3. **Сразу видит сводку вверху:** «180: 23 шт | 150: 19 шт | 60×80: 1 шт | Итого: 141 кв.м»
4. Готовит коврики по списку

**Критерий:** Сводка по размерам — это **первое, что видно** на экране маршрута, не нужно скроллить.

### US-2: Печать маршрутного листа для Ильи (утро)

**Ситуация:** Утром админ печатает маршрутный лист и отдаёт Илье.

**Шаги:**
1. Открывает приложение → маршрут на сегодня уже на экране
2. Жмёт «Печать»
3. Получает чистую таблицу на A4 в привычном формате (названия как в Excel)
4. Отдаёт Илье

**Критерий:** От открытия до печати — **2 клика** (открыл → Печать → ОК в диалоге принтера).

### US-3: Быстрый поиск клиента в маршруте

**Ситуация:** Звонит клиент «Монетка на Кирова», нужно быстро найти его в сегодняшнем маршруте.

**Шаги:**
1. На экране маршрута вводит «монетка» или «кирова» в поле поиска
2. Список фильтруется, видна нужная точка
3. Может убрать из маршрута / перенести / посмотреть детали

**Критерий:** Найти клиента в маршруте — **менее 5 секунд**.

### US-4: Новый клиент — добавить в базу и в маршрут

**Ситуация:** Подписали нового клиента, нужно добавить его в систему и вставить в маршрут на среду между точками 15 и 16.

**Шаги:**
1. Переходит в «Базу клиентов» → «Добавить клиента»
2. Заполняет: название, адрес, коврики (размер + кол-во), частота, дни
3. Сохранение автоматическое
4. Переходит на маршрут Среды → «+ Добавить точку»
5. Находит нового клиента в списке, выбирает «Вставить после точки №15»
6. Клиент встаёт на позицию 16, остальные сдвигаются

**Критерий:** Вставка в середину маршрута — **без перетаскивания через 40 позиций**. Выбор позиции при добавлении.

### US-5: Клиент просит перенести на другой день

**Ситуация:** «Мы на ремонте в среду, приезжайте в четверг на этой неделе.»

**Шаги:**
1. На маршруте Среды находит клиента (поиск)
2. Жмёт «Перенести» на карточке точки
3. Выбирает «Четверг» и позицию (или «В конец»)
4. Точка исчезает из среды, появляется в четверге

**Критерий:** Перенос между днями — **3–4 клика**, не нужно вручную удалять и заново добавлять.

### US-6: Убрать точку на эту неделю (клиент закрыт / отпуск)

**Ситуация:** Клиент предупредил, что на этой неделе их не будет.

**Шаги:**
1. Находит клиента в маршруте (поиск)
2. Жмёт ✕ → подтверждение «Убрать из маршрута на сегодня?»
3. Точка убрана, нумерация пересчитана
4. Клиент **остаётся в базе** и в маршрутах на следующие недели

**Важно:** «Убрать из маршрута» ≠ «Удалить клиента». Это разные действия.

### US-7: Проверка — все ли клиенты на месте

**Ситуация:** Админ хочет убедиться, что ни один клиент не потерялся при переносах.

**Шаги:**
1. Открывает «Базу клиентов»
2. Фильтрует по дню (напр. «Среда»)
3. Видит всех клиентов, у которых среда в расписании
4. Сравнивает с маршрутом среды — если кто-то выпал, добавляет обратно

**Критерий:** В базе клиентов видно, включён ли клиент в маршрут на каждый из своих дней.

---

## 7. Технические решения

### 6.1 Стек

| Компонент | Технология | Почему |
|-----------|-----------|--------|
| Фронтенд | React (один .jsx файл) | Интерактивность, можно запустить как артефакт |
| Стилизация | Tailwind CSS | Быстрая разработка, адаптивность |
| Хранение данных | localStorage + JSON экспорт | Одно устройство, локально, с бекапом в файл |
| Печать | CSS @media print | Нативная печать через браузер |

### 6.2 Хранение данных

**Решение: localStorage + файловый экспорт**

Поскольку приложение используется локально на одном устройстве:

- **localStorage** — основное хранилище, данные сохраняются в браузере между сессиями
- **Экспорт/импорт JSON** — кнопки «Сохранить копию» и «Загрузить из файла» для бекапа и подстраховки
- Автосохранение при каждом изменении (без кнопки «Сохранить»)

### 6.3 Миграция данных

При первом запуске приложение загружается с данными, извлечёнными из текущего Excel-файла. Данные парсятся и нормализуются в новую модель (раздел 3).

**Что нужно распарсить из названий клиентов:**
- Название организации (например, «Метрополис»)
- Адрес (например, «К. Маркса»)
- Размеры ковриков (например, «2x180, 2x150»)
- Цвет/тип (например, «коричневый», «новый», «старый»)
- Примечания (например, «3-ий вход», «резаный»)

**Важно:** Исходная строка из Excel (например, «метрополис к. Маркса -2x180, 2x150») сохраняется как поле `originalName` и используется для отображения в маршруте и печатном листе. Структурированные поля — для базы клиентов и фильтров.

**Экран проверки после импорта:**
Парсинг будет приблизительным, поэтому после импорта нужен экран сверки:
- Таблица: «Исходная строка из Excel» → «Что распознали: название / адрес / коврики»
- Строки с проблемами (не удалось распознать) подсвечены
- Возможность быстро поправить прямо в таблице
- Кнопка «Всё верно, запустить»

---

## 8. Этапы реализации

### Фаза 1 — MVP (приоритет)

**Цель:** Заменить ежедневное использование Excel. Покрывает сценарии US-1 — US-7.

| Функция | Сценарий | Приоритет |
|---------|---------|-----------|
| Маршрут на день при открытии (не дашборд) | US-1, US-2 | 🔴 Критично |
| Сводка ковриков по размерам вверху экрана | US-1 | 🔴 Критично |
| Переключатель дней (ПН–ПТ) | US-1, US-5 | 🔴 Критично |
| Список точек с порядком объезда | US-2 | 🔴 Критично |
| Поиск по маршруту | US-3, US-5, US-6 | 🔴 Критично |
| Печать маршрутного листа (привычные названия) | US-2 | 🔴 Критично |
| Порядок точек (↑↓ и drag & drop) | US-4 | 🔴 Критично |
| Добавить точку в позицию (не только в конец) | US-4 | 🔴 Критично |
| Убрать точку из маршрута (без удаления клиента) | US-6 | 🔴 Критично |
| Перенести точку в другой день | US-5 | 🔴 Критично |
| Чекбокс «Выполнено» | US-2 | 🟡 Важно |
| Импорт данных из Excel + экран проверки | — | 🔴 Критично (однократно) |
| Сохранение данных (localStorage) | — | 🔴 Критично |

### Фаза 2 — Управление

**Цель:** Полностью уйти от Excel.

| Функция | Приоритет |
|---------|-----------|
| База клиентов (просмотр + поиск) | 🔴 Критично |
| Добавление клиента | 🔴 Критично |
| Редактирование клиента | 🔴 Критично |
| Удаление клиента | 🟡 Важно |
| Добавить/убрать точку из маршрута | 🔴 Критично |
| Перенести точку в другой день | 🟡 Важно |

### Фаза 3 — Улучшения

| Функция | Приоритет |
|---------|-----------|
| Экспорт/импорт JSON (бекап) | 🟡 Важно |
| Оптимизация маршрутов (порядок объезда) | 🟡 Важно |
| Привязка к карте / визуализация маршрута | 🟢 Желательно |
| История изменений | 🟢 Желательно |
| Пауза клиента | 🟢 Желательно |
| Статистика за период | 🟢 Желательно |
| Адаптив под мобилку | 🟡 Важно |

---

## 9. Открытые вопросы (с ответами)

| # | Вопрос | Ответ / Решение |
|---|--------|----------------|
| 1 | Размеры ковриков — откуда брать? | Размер является частью имени клиента в Excel (напр. «Велес гоголя **180**»). Площади: 180→2.07, 150→1.275 и т.д. |
| 2 | Логика подмаршрутов/блоков? | Блоков нет — маршрут на день это плоский список остановок. Оптимизация порядка объезда — пост-MVP |
| 3 | Секция «Машиностроителей» — отдельный рейс? | «Машиностроителей» — просто название улицы в адресах клиентов, точки включены в общий список |
| 4 | Доступ с нескольких устройств? | Нет. Локально, одно устройство — достаточно |
| 5 | Кто пользователь? | **Админ** (владелец) — основной. **Илья** — курьер, получает печатный лист. Приоритет UX = админ-панель |
| 6 | «Алые паруса 4×400 и 3×240» | 4 коврика по 400 см + 3 коврика по 240 см |
| 7 | Привязка к карте? | Не в MVP. Можно добавить позже |

---

## 10. Критерии приёмки

**MVP считается готовым, когда:**

1. При открытии приложения сразу виден маршрут на текущий день со сводкой ковриков по размерам вверху
2. Переключение между днями — 1 клик, сводка пересчитывается
3. Сводка «сколько ковриков подготовить» видна без скролла (US-1)
4. От открытия до печати маршрутного листа — 2 клика (US-2)
5. Печатный лист: привычные названия из Excel, крупный шрифт, итоги
6. Поиск клиента в маршруте — менее 5 секунд (US-3)
7. Вставка новой точки в конкретную позицию маршрута — без перетаскивания через весь список (US-4)
8. Перенос точки между днями — 3–4 клика (US-5)
9. «Убрать из маршрута» не удаляет клиента из базы (US-6)
10. Все 143 клиента и 5 маршрутов импортированы, экран проверки после импорта
11. Данные сохраняются при закрытии браузера
